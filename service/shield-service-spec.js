describe('shieldService', function() {

  beforeEach(module('shield'));

  it('should ...', inject(function(shieldService) {

    //expect(shieldService.doSomething()).toEqual('something');

  }));

});
